--- /dev/null
+++ b/modules.ini
@@ -0,0 +1,26 @@
+; You can disable any extension below by commenting it with a semicolon and by
+; restarting Apache2. Be aware that some modules can depend of other and thus
+; loading order is signifiant.
+;
+; In case an needed extension is missing you can create a ticket at http://youtrack.i-mscp.net
+
+extension = curl.so
+;extension = domxml.so
+extension = gd.so
+;extension = gmp.so
+extension = imap.so
+;extension = ldap.so
+extension = mcrypt.so
+;extension = mhash.so
+;extension = pspell.so
+;extension = recode.so
+;extension = snmp.so
+;extension = xmlrpc.so
+
+; Database extensions
+
+extension = mysql.so
+;extension = odbc.so
+;extension = pgsql.so
+;extension = mssql.so
+;extension = interbase.so
